# jsdom@16 declara canvas@^2 como peer OPCIONAL y el repo usa canvas@3 (funciona: jsdom
# solo lo usa si existe). Sin esto, npm ci / npm install fallan por el conflicto de peers —
# es el mismo --legacy-peer-deps que ya pedía el README, hecho permanente para humanos y CI.
legacy-peer-deps=true
